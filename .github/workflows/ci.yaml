name: Build and Push

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: my-web-app

jobs:

  build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to JFrog
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.JFROG_REGISTRY }}
          username: ${{ secrets.JFROG_USERNAME }}
          password: ${{ secrets.JFROG_TOKEN }}

      - name: Build image
        run: |
          docker build \
            -t ${{ secrets.JFROG_REGISTRY }}/docker-local/${IMAGE_NAME}:${{ github.run_number }} .

      - name: Push image
        run: |
          docker push \
            ${{ secrets.JFROG_REGISTRY }}/docker-local/${IMAGE_NAME}:${{ github.run_number }}